package com.heru.httpwww.kurio_messanger.Login.loginView;

import android.content.Intent;

public interface loginView {
    void IntentToRegister(Intent intent);

    void intenToMain(Intent intent);
}
